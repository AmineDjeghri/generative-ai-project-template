# -- DEV MODE if true, log debugs and traces
DEV_MODE=True

# -- FASTAPI
FASTAPI_HOST=0.0.0.0
FASTAPI_PORT=8000

# -- FASHN (Virtual Try-On API)
# Obtain an API key from https://fashn.ai/products/api and set it below.
FASHN_API_KEY=
# Optional overrides
FASHN_MODEL_NAME=tryon-v1.6
FASHN_BASE_URL=https://api.fashn.ai/v1
FASHN_POLL_INTERVAL=2.0
FASHN_TIMEOUT_SECONDS=180

# -- QWEN / ComfyUI (Local Image Edit / Try-On Graph)
# Enable Qwen/ComfyUI provider and set your ComfyUI server URL
QWEN_ENABLED=False
COMFYUI_SERVER_URL=http://127.0.0.1:8000

# -- Standalone NiceGUI UI
# Host/port for the UI and where it should call the API (default points to the local backend)
UI_HOST=127.0.0.1
UI_PORT=8080
